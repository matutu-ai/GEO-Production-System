services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${API_BASE_URL:-https://api.housun.shop}

  backend:
    environment:
      APP_ENV: production
