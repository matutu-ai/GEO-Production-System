# GEO Analysis Tests

Run from the repository root:

```bash
cd backend
.venv/bin/pytest -q ../apps/geo-analysis/tests
```

Tests cover article analysis, entity extraction, keyword clustering, intent analysis, framework generation, SVG generation, all exporters, and the full pipeline.
