# SVG Renderer

Generates a standalone SVG architecture diagram with groups `title`, `layers`, `nodes`, `edges` and `labels`.

The SVG uses a viewBox, inline styles, and standard SVG text, so it can be embedded in HTML, rendered in the browser, downloaded, and converted to PDF/PNG.
